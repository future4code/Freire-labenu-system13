// import { Request, Response } from "express";
// import { connection } from "../../connection";
// import { buscarTurma } from "./buscarTurma";

// export default async function mudarModulo(req: Request, res: Response) {
//   try {
//     const {idEstudante, turma} = req.body
//    if (!idEstudante || !turma){
//     throw new Error("id e turma não informados");
//     }

//   const turmaExiste = await buscarTurma("Turma");

//   if(!turmaExiste){
//     throw new Error(`Turma ${idTurma} não existe`);

//   }

//   const estudanteExiste = await buscarEstudante

//   if()

//   }

// };
